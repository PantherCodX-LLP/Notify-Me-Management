/node_modules
/.next
/out
/build
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
*.pem

# Local env files (contain DB credentials) — never commit
.env.local
.env*.local

# Inspector output (may contain sample customer data)
schema-dump.json
schema-report.md

# Docker compose env + rclone credentials
.env
docker/rclone.conf
